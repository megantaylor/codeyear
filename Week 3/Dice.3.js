// pick a random number between 1 and 6 for our roll of the die
var die = Math.floor(Math.random()*6 + 1);
var score;

score = die; // set score here equal to die

console.log("You rolled a "+die+" for a score of "+score);
