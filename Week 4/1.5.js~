var myFunction = function (parameter) { if (condition) {code;} else {code;} };


var myFunction = function (parameter) { 
    if (condition) {
        code;  
    } 
    else {
        code;    
    } 
};
console.log("Function code goes between curly brackets.");
